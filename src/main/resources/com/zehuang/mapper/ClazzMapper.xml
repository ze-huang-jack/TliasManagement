<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zehuang.mapper.ClazzMapper">
    <select id="list" resultType="com.zehuang.pojo.Clazz">
        select clazz.*,
        emp.name masterName
        from clazz
        left join emp on clazz.master_id = emp.id
        <where>
            <if test="name != null and name != '' ">
                and clazz.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                AND clazz.end_date BETWEEN #{begin} AND #{end}
            </if>
        </where>
        order by update_time desc
    </select>

    <insert id="insert">
        insert into clazz(name, room, begin_date, end_date, master_id, subject,
                          create_time, update_time)
        VALUES (#{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject},
                #{createTime}, #{updateTime})
    </insert>

    <select id="getById" resultType="com.zehuang.pojo.Clazz">
        select id,
               name,
               room,
               begin_date,
               end_date,
               master_id,
               subject,
               create_time,
               update_time
        from clazz
        where id = #{id}
    </select>

    <update id="update">
        UPDATE clazz
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="room != null and room !=''">
                room = #{room},
            </if>
            <if test="beginDate != null">
                begin_date = #{beginDate},
            </if>
            <if test="endDate != null">
                end_date = #{endDate},
            </if>
            <if test="masterId != null">
                master_id = #{masterId},
            </if>
            <if test="subject != null">
                subject = #{subject},
            </if>
            update_time = now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        delete
        from clazz
        where id = #{id}
    </delete>

    <select id="selectAll" resultType="com.zehuang.pojo.Clazz">
        select id,
               name,
               room,
               begin_date,
               end_date,
               master_id,
               subject,
               create_time,
               update_time
        from clazz
        order by update_time desc
    </select>
</mapper>